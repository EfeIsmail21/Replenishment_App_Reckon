<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <!-- Title at the top - "REPLENISHMENT APP" -->
    <TextView
        android:id="@+id/app_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:gravity="center"
        android:padding="8dp"
        android:text="REPLENISHMENT APP"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:fontFamily="sans-serif-condensed" />

    <!--Placeholder for the ActionMenu which will be centered -->
    <Space
        android:id="@+id/menu_placeholder"
        android:layout_width="match_parent"
        android:layout_height="@dimen/action_menu_height"
        android:layout_centerInParent="true"/>

    <!-- User greeting that will appear at the bottom -->
    <TextView
        android:id="@+id/main_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="50dp"
        android:gravity="center"
        android:textSize="16sp"
        android:textColor="@color/vuzix_green"
        android:fontFamily="sans-serif-condensed"
        android:text="" />

    <!-- Keep this for compatibility but make invisible -->
    <TextView
        android:id="@+id/main_value"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <!-- Keep this for compatibility but make invisible -->
    <ImageView
        android:id="@+id/icon"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        tools:ignore="ContentDescription" />

</RelativeLayout>