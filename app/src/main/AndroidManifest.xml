<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:usesCleartextTraffic="true"
        android:theme="@style/HudTheme"
        tools:replace="android:theme">
        
        <!-- Login screen (entry point) -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Main application screens (Will inherit HudTheme from application) -->
        <activity android:name=".ui.main_menu.MainMenu" 
                  android:exported="false"/>

        <activity android:name=".ui.machine.MachineMenu"
                    android:exported="false"/>

        <activity android:name=".ui.settings.SettingsMenu"
                  android:exported="false"/>
                  
    </application>
</manifest>